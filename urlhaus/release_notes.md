#### The following enhancements have been made to the URLhaus Connector in version 1.1.0:

- Added the `Auth Key` parameter to the Configuration section.
- Renamed the connector label from `URLHaus` to `URLhaus`.
